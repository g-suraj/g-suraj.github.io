---
const { index, link, border } = Astro.props;
---

<a href={link} target="_blank">
  {
    (
      <div
        id={index}
        class:list={[
          "bubble",
          "bg-base01",
          "text-base06",
          "border-4",
          "rounded-xl",
          "h-fill",
          border || "border-base0A",
        ]}
      >
        <div class="p-4">
          <slot />
        </div>
      </div>
    )
  }
</a>
